# Workflow

1. fare la fetch
    - salvo la variabile api_url
    - fetch con promise1 e promise2, catch e finally
    - log dei dati
2. fare la card
    a partire dalla struttura dati emersa dal log:
        - estrapolo i vari dati dell oggetto attore
        - li spiaccico in un componente
        - (con un forEach ?) creo le card
3. fai la grafica della card